# Generated documentation for module arcpy.management


class LoadDataUsingWorkspace(object):
    """
    Uses the Data Reference Workbook from the Data Loading Workspace to load data from a source to a target dataset.
    """

    @property
    def description(self) -> str:
        return """

        LoadDataUsingWorkspace_management(in_workbook)

        Uses the Data Reference Workbook from the Data Loading Workspace to
        load data from a source to a target dataset.

     INPUTS:
      in_workbook (File):
          The path to the Data Reference Workbook defining the data source,
          target, and mapping workbook paths.

        """